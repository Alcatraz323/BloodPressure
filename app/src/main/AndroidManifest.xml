<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="io.alcatraz.bloodpressure">

    <uses-permission android:name="android.permission.VIBRATE" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:name=".BloodPressureApplication"
        android:theme="@style/AppTheme">
        <activity
            android:exported="true"
            android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:exported="false" android:name=".activities.PreferenceInnerActivity" />
        <activity android:exported="false" android:name=".activities.PreferenceActivity" />
        <activity android:exported="false" android:name=".activities.LogActivity" />
        <activity android:exported="false" android:name=".activities.AboutActivity" />
        <activity android:exported="false" android:name=".activities.MeasureActivity" />
        <activity android:exported="false" android:name=".activities.DeviceInfoActivity" />
        <activity android:exported="false" android:name=".activities.HistoryActivity" />
    </application>

</manifest>